# ABRIAT Landing Page

Landing page React + Vite + TypeScript para captação de interesse na ABRIAT.

```bash
npm install
npm run dev
npm run lint
npm run typecheck
npm test
npm run build
```

Consulte `docs/abriat/` para limites de conteúdo, assets, eventos e pendências de publicação.
